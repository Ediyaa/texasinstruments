/*
 * console_commands.c
 *
 * Abgeloest durch console_commandsv2.c (Dispatch-Tabelle statt
 * if/else-Kette). Inhalt bewusst geleert, um doppelte Definitionen
 * von commands()/listCommands() beim Linken zu vermeiden.
 * Alte Fassung steht in der Git-Historie.
 */
